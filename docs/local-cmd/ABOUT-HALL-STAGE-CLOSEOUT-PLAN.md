# 首页赛车 × About 馆阶段性收口执行计划

> 当前入口：`ABOUT-HALL-INDEX.md`。本文件记录已批准的有限交付范围，不另建第二份状态表。
> 执行状态（2026-09-04 05:28）：S0–S5 已完成。final-r2 109/109、LHCI 24/24、视觉 8/8/8；PR #234、topic/main CI、Pages 与线上动线均闭合。真机 iPhone Safari 按计划单项记 `PARTIAL`。

## 目标

交付一条完整动线：在首页认出 About 楼，驾驶并用 Q/E 环视，泊车进站，进入六站展厅，完成“回家”变身，再前往纸面介绍或返回城市。

## 非目标

本期不重开真人路线，不给手机恢复竖版视频，不编造真实履历，不做第二栋楼、全城重做或全站提分。

## 顺序

1. 保全 attempt10 日志、trace、截图、视频、环境和历史截图备份。
2. 在 Node 22、锁文件和 pnpm 10 下分别诊断 `CITY-E2E-05` 与 `WS-E2E-03`，不加 timeout、retry 或 skip。
3. 完成 About 招牌、手机静帧扫光、问题卡摘要、竖版禁入、展厅 Lighthouse 接线和大分包归因。
4. 重拍最终首页桌面/手机静帧，统一产物做一次视觉复议和一次针对发现的核验。
5. 冻结源码、配置、媒体和 `dist`，运行真实聚合门与 7 个 Playwright 项目；只接受 0 failed、0 skipped、0 flaky、退出码 0。
6. 提交、推送并更新 PR #234；CI 绑定最终 `head_sha` 后合入，核对 Pages 部署提交与线上完整动线。
7. 更新索引、LOOP-LOG、架构、启动提示词、交接和经验，关闭十分钟监控。

## 停止条件

有限清单全部闭合即 `DONE`。硬条件缺失时标 `PARTIAL`，逐项写清缺失证据、补充者与复验方式，不用“基本完成”替代失败。
