# CC-LOOP-ADVISOR-T6 Tick#6 扇出裁决（fanout）

- **顾问**：CC-LOOP-ADVISOR-T6（model slug: `claude-fable-5-thinking-xhigh`）
- **时间**：2026-08-28 03:30–03:35 UTC（Tick#6）
- **基线**：main @ `88097f9`（本 tick `git fetch` 实测，自 Tick#2 无新合入）
- **纪律**：零 src/ 改动，本文档为唯一交付物；产出于独立 worktree `/tmp/t6-wt`
  （base = origin/main），未触碰 `/workspace` 共享检出、未杀任何在跑进程。登记看板
  单源仍为 `docs/research/cyber-city-score-loop-orchestration.md`，本文不重复登记分数。

---

## 0. 事实核查（fresh 取证，全部本 tick 03:30–03:33 实测）

| 对象 | 实测状态 | 取证方式 |
|------|---------|---------|
| main | `88097f9`，无新合入 | `git fetch` + `rev-parse origin/main` |
| #104 X2 | draft，tip `c24c7f3` 静止；门禁在现 tip SUCCESS（03:00:44） | `gh pr view 104` |
| X2 进程 | **实时活跃**：`pnpm test:e2e`（PID 8039）已跑 34min+，双 worker 在飞，trace 03:31:47 仍在写 | `ps -o etime` + `find -printf %T@` |
| X2 e2e 战况 | **失败已从 T5 的 1 例扩至 5 例**（明细见 §2.1）；本轮 52/52 已无可能 | test-results 逐目录实测（error-context.md 存在性判失败；03:05 c5677 目录仅含 attachments，非失败） |
| #103 FXN-R7 | ready（非 draft），远程 tip 仍 `c4e844c`；Codex 本地 `862ab26` ahead 9 未 push | `gh pr view 103` + `git status -sb` |
| Codex 进程 | **实时活跃且进展良好**：run3（PID 16395，03:22:20 起）已越过 run2 失败点（03:28:38 ✔ hint-recall{via:button}），03:31:27 已达 shot-apply/POI 段，out/ 已采 7 PNG | run3.log 逐行实读 + out/ 目录实测 |
| #112 SEC-P4 | draft，tip `3815160`，门禁 SUCCESS（03:09:59），**仍待指挥官人工合入** | `gh pr view 112` |
| VM 负载 | **4 核，load average 4.94 / 9.50 / 9.00**（1/5/15min）——持续 2.2–2.4 倍超订；内存充裕（free 10.9GB） | `uptime` + `nproc` + `free` |
| VM 硬护栏 | X2 + Codex + 本顾问 = 3/3；本件收口即释放 1 槽 | 进程实测 |
| /workspace 残留 | 仍挂污染 ref `cursor/cc-loop-advisor-t2-5b71` @ `b85bf85`（ahead 4，内容为 X2 立面套件提交），T3 §4.2 处置仍未执行；维持 T4 §4 原判 | `git status -sb` + `worktree list` 实测 |

---

## 1. 本 tick 父代理动作：**默认继续等，零打扰**（维持 T5 §1 裁决）

1. **不 resume 任何一路**。两路进程均实时活跃（X2 trace 03:31 在写、Codex run3
   03:31 在推进），活性锚双双刷新至 **03:31**，距任何阈值档 ≥35 min（见 §4.1）。
2. **不杀、不暂停任何进程**（含 §3 的错峰议题——只影响*下一轮*排程，不动本轮）。
3. **不开新重载 Task**。VM 3/3 无空槽且 load 已 2.3 倍超订，T5 §1 排程约束升级为
   §3 的互斥建议。
4. 唯一动作 = 收下本文档 PR + 把 §3 排程约束与 §2 门控更新传达给 X2 / T6-A 相关
   任务书。#112 合入仍属指挥官人工窗口，父代理不代合。

---

## 2. CITY-EXP-01 门控更新：失败集已扩容且**分裂为两类**，X2 ready 门槛加硬

### 2.1 本轮失败明细（5 例，两类签名）

| # | 用例 | 时间 | 失败签名 | 定性倾向 |
|---|------|------|---------|---------|
| 1 | CITY-EXP-01 探索计数（重复进圈去重→reload 持久还原） | 03:14:57 | **语义断言**：「泊车位 (-28,-28) 应可达（实测 x=23.5 z=-32.9）」——车没开到位，非崩溃 | 待 triage：R2 立面套件/前景景框碰撞体挡道的**真回归嫌疑未排除**；也可能是竞争下物理步进失真 |
| 2 | 探索-目标线：链首→空闲 nudge 呈现 | 03:29:30 | 崩溃簇 | 环境倾向 |
| 3 | 反馈-驾驶：spawn toast→翻车倒计时→测速牌 | 03:30:13 | 崩溃簇 | 环境倾向 |
| 4 | 反馈-触屏：「操作说明」召回按钮 | 03:31:47 | **`Error: Channel closed`** + host attr 读空 | 环境倾向 |
| 5 | 反馈-键位卡：首驶阅读窗→淡出→H 再唤出 | 03:31:47 | **`Error: Channel closed`**（与 #4 同秒双挂，worker 级联崩溃特征） | 环境倾向 |

**环境定性证据链**：`Channel closed` 是 renderer/worker 崩溃签名而非断言失败；#4/#5
同一秒双挂；4 核 VM load 9+；且 #2–#5 全部落在 Codex run3（03:22 起）重载窗口内，
#1 落在 run2 窗口（T5 §0：run2 03:14 失败时正在飞）。

### 2.2 X2 ready 门槛更新（在 T5 §2 基础上加硬三条）

1. **复跑必须独占窗口**：干净复跑 52/52 的证据仅在**无并发 capture/重载**时有效
   （复跑窗口需与 Codex capture 线错峰，取证= 复跑起止时间戳 vs `ps`/run log）。
   本轮已实证共载会制造崩溃簇，共载复跑证据一律不采。
2. **处置说明覆盖全部 5 例**：X2 收口报告不可只答 CITY-EXP-01——#2–#5 可按环境
   崩溃簇打包定性（引§2.1 证据链），但 **#1 必须单独 triage**：其失败语义（车没
   开到泊车位）与 R2 新增碰撞体的回归模式吻合，须给出「独占窗口复跑通过 → flake
   定性」或「碰撞体排查 → 修复 commit + 绿门禁」二选一的明确证据。
3. **T6-A 审计清单增补**（叠加 T5 §2 原文）：核对 X2 复跑窗口独占性证据；若 X2
   声称 52/52 而复跑窗口与任何 capture/重载进程重叠，或未逐类处置 5 例留痕，**打回**。

### 2.3 ready 预估再后移

e2e 本轮 03:31+ 仍在跑（已 35min+，受共载拖慢）。flake 路径 = 本轮跑完（~03:40–03:50）
+ triage + **独占复跑 17–23min**（还需等 Codex capture 线让出）≥ **04:30**；真回归路径
（#1 判碰撞体挡道）= 改码 + 门禁重跑 ≥ **05:00**。T6-A 最早 **Tick#9–#10** 达成入场。

---

## 3. 同 VM 双负载错峰：**建议采纳（仅排程约束，不动本轮进程）**

**结论：本轮两路都让它跑完，不杀不停；从下一轮起强制错峰。**

- **本轮不干预的理由**：X2 本轮 52/52 已无可能（§2.1），现在停 capture 救不回本轮
  e2e；Codex run3 正处最接近成功的一次（已越过 run1/run2 失败点），中断=前功尽弃
  且违反「勿杀进程」纪律。本轮 e2e 残值 = 完整失败清单（triage 素材），让它跑完。
- **从下一轮起的互斥规则（供父代理写进任务书）**：
  1. 本 VM 上「全量 e2e」与「capture/ffmpeg 取证」为**互斥重载**，同一时刻至多一
     路在跑；开跑前先 `ps` 查 playwright/ffmpeg/chrome-headless 存量。
  2. 顺序裁决：**Codex capture 线优先收尾**（run3 已在 POI 段，预计先完），X2 独占
     复跑排其后——这同时满足 §2.2-1 的独占窗口要求。
  3. 该规则是 T5 §1「重载缓开」的升级：从「新任务缓开」升级为「存量重载也须串行」。
- **归因备忘**：若 X2 独占复跑通过，则本轮 5 例中 #2–#5 环境定性坐实，同时反证
  互斥规则必要性；归因写入 X2 收口报告，供 T6-A 复核。

---

## 4. Tick#7 预排（~03:40 UTC）

### 4.1 阈值档：**不触发 resume**

| 路 | 活性锚（本 tick 刷新） | 软检 T+35 | resume T+50 | 重派 T+65 | Tick#7（03:40）判定 |
|----|----------------------|-----------|-------------|-----------|--------------------|
| X2 | trace 03:31:47 在写 → **03:31** | 04:06 | 04:21 | 04:36 | 未达任何档；T5 口径的 03:56 软检已被本 tick 取证提前完成且顺延 |
| Codex | run3.log 03:31:27 在推进 → **03:31** | 04:06 | 04:21 | 04:36 | 未达任何档；T5 口径 03:57/04:12 档随锚刷新作废 |

resume 最早考虑点顺延至 **04:21（约 Tick#11）**，且仅当「进程已退 ∧ 无 push ∧
无新 commit ∧ 无新留痕」全链成立才触发（T4 §1.3 口径不变）。Tick#7 大概率是
**结局驱动 tick** 而非阈值 tick——两路预计在 03:35–03:50 间先后见分晓。

### 4.2 Tick#7 任务面（2–4 路，全部 `claude-fable-5-thinking-xhigh`）

| # | 任务 | 触发条件 | 动作 | 串并行 |
|---|------|---------|------|--------|
| T7-A | X2 e2e 收轮跟进 | PID 8039 退出 | 核最终失败计数与退出码 → 把 §2.2 三条硬门 + §3 独占复跑顺序传达 X2；X2 无反应则按 4.1 阈值走 | 与 T7-B 并行（零文件交集） |
| T7-B | Codex run3 结局跟进 | run3 成功 → 预期 9 commits push | 核新 tip 门禁 → SUCCESS 则报指挥官合 #103 → 功能登记 87 走秘书线（T4 §5 原文）；run3 失败 → 看是否自动 run4，锚随之刷新，仍不 resume | 与 T7-A 并行 |
| T7-C | T6-A 视觉审计（顺延件） | X2 IDLE ∧ #104 ready ∧ 门禁 fresh 绿 ∧ §2.2 处置证据齐 | 按 T5 §4 T6-A 原文 + §2.2-3 增补清单入场；预计最早 Tick#9–#10 | 严格压后 |
| T7-D | SEC-P5 秘书刷板 | 见 §5 触发口径 | 见 §5 增量策略 | 压后，材料齐才动 |

VM 预算：本顾问收口释放 1 槽 → Tick#7 峰值 = X2（在途）+ Codex（在途）+ 0 新重载
≤3 ✅。逻辑队列 = X2 收轮 triage、Codex push 连锁、#112 人工窗口、T6-A、SEC-P5
≈ 5 项，带内（2–6），无积压。

---

## 5. 每 3 tick 看板：SEC-P5 要点（Tick#6 = 3-tick 界点）

### 5.1 现状判定：**本 tick 无板面级增量，SEC-P5 暂不开**

看板登记面（分支/PR/分数/裁决）自 P4（tip `3815160`）以来**零变化**：main 未动、
#103/#104 tip 未动、无新审计分、无合流。两路进程的中途战况属过程性信息，按单源
纪律不上板（收口才登记）。空刷 = 制造与 #112 的无谓 diff。

### 5.2 #112 未合时的增量策略（供 SEC-P5 任务书）

1. **首选：材料齐后在 #112 同分支追加 commit**。秘书线拥有
   `cursor/cc-loop-sec-p4-5b71`，板面新增量（如 #103 合入、X2 triage 裁决落地）
   直接以 P5 commit 追加到 #112——单 PR 单源，避免开第二个看板 PR 造成双源，也
   不属于 AGENTS.md 禁的 PR 栈场景（同一 PR 延展，非叠栈）。追加后门禁会重跑，
   指挥官合入窗口顺延可接受（#112 本就非阻塞件）。
2. **若指挥官在追加前已合 #112**：SEC-P5 按 T5 §4 T6-C 原文从合流后 main 新拉
   `cursor/cc-loop-sec-p5-5b71`。
3. **触发口径（满足任一才开工）**：① #103 合入或 tip 变更 + 门禁结果；② #104
   undraft / X2 收口报告出炉（含 5 例处置）；③ #112 被合入（登记自身合流 + close
   #109 superseded 连锁）；④ 累计 2 个以上 PR 状态变化。
4. **P5 待登记清单（预填）**：#112 合流状态；#103 合入 + 新 tip；X2 5 例失败
   triage 裁决与复跑证据指针；功能登记 87（走 T4 §5 秘书线，须 #103 合入后）；
   VM 共载事故与 §3 互斥规则（登记为排程约束，非分数项）。

---

*本文档为 CC-LOOP-ADVISOR-T6 Tick#6 交付物；登记看板不在本文更新，由秘书线单源维护。*
