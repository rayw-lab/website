# CC-LOOP-ADVISOR-T26 · 事件驱动裁决单（run4 决定性 ✘ 定性 + 判读 B 触发 + 回炉排序）

- **角色**：CC-LOOP-ADVISOR-T26（事件驱动编排顾问，非每 tick 洪水；触发事件 = 真空重跑收轮 ✘，属 #143 §4-2 授权清单「任一趟 ✘」）
- **model slug**：`claude-fable-5-thinking-xhigh`
- **取证窗口**：2026-08-28 07:12–07:16 UTC
- **纪律**：零 `src/`/`e2e/` 改动；独立 worktree `/tmp/worktrees/t26`（base = main `9177dcc`）；全程未杀任何进程、零 chrome 级活动、未 resume ENV

---

## 0. 取证域声明（一手 / 转述边界）

本顾问 VM 为新开机器（uptime 1 min），与 ENV 代理 VM **不同机**：`/tmp/evidence-exp01/`、`/tmp/env-exp01-run3.log`、`/tmp/env-wt/test-results/` 在本机不存在，**无法一手复核**。证据分级如下：

| 级别 | 证据 | 来源 |
|------|------|------|
| **一手（本单实测）** | main tip = `9177dcc`；#146 `OPEN / ready / MERGEABLE / CLEAN / CI SUCCESS`；#147/#129/#134/#104/#43 状态；pr-129-head = `89d1cbb`（base `88097f9`）；**途径点 `(26,-8)` 仅存在于 pr-129 谱面（main 零匹配）**；ENV 面板 `bc-53ac6339-…ca99` = RUNNING（batch-fetch 07:13Z，事件流空） | `git`/`gh`/面板 API |
| **转述（父代理 07:10Z 通报）** | `vacuum-run3.txt` @06:57:02Z PASS（chrome=0 / load1=0.02 / astro=0，进程名精确版）；tmux `env-exp01-run3` 06:57→~07:05 收轮；`RUN3_EXIT=1`、CITY-EXP-01 1 failed（8.5m）；失败断言「途径点 (26,-8) 不可达，实测 x=27.4 z=-21.8」；产物在 `/tmp/env-wt/test-results/` | ENV VM 文件，本单不可达 |
| **旁证（T25/#134 一手记录）** | T25 F7：06:52/06:56 独立 ps 实测真实计数 = 0、load 0.03；T25 §2.3-1 点名三查脚本自匹配修法；#134 报告：main@`88097f9` 对照跑（04:14–04:26）EXP-01 同败，卡 `(25.2,-25.7)` 500s 挪 1.4m 纯爬行；SwiftShader 实测 0.699–0.778 fps | PR #147 / #134 文档 |

---

## 1. 登记矩阵（看板单源口径，本单不冒登）

| 维度 | 北极星 | 生产登记 | Δ | 在途 |
|------|--------|----------|---|------|
| **综合** | **98** | **80** | +18 | COMP-M0 重算 |
| **视觉** | **98** | **73** | +25 | 董事会 R1 §6 定谳 73（71 为 stale 笔误口径）；#104 冻结 |
| **功能** | **90** | **84** | +6 | **#146 CI 绿 + ready + MERGEABLE（gh 一手复验），合入权在指挥官；合前生产登记仍读 84，任何角色禁冒登 87** |
| **性能** | **85** | **—** | +85 | 未登记；解锁条件 = 真机 human-gate 六腿 → AL-PERF |

---

## 2. 裁决 A：本趟定性 = **run4 决定性第一腿（leg1）✘**，非降级诊断

### 2.1 决定性资格成立（三要件齐）

1. **起飞前真空档 PASS**（06:57:02Z，面值 0/0/0）。**采信裁决：可信为决定性起飞条件**，理由三条：① T25 F7 已于 06:52/06:56 两次一手独立 ps 实测真实计数 = 0，06:57:02 面值 0 与之吻合；② 「进程名精确版」正是 T25 §2.3-1 点名的修法（排除 wrapper 自匹配 / pgrep 精确匹配），修复方向可追溯、非凭空清零；③ load1=0.02 与 T25 F5 的 0.03 同量级。采信级 = **转述 + 旁证充分**（本单 VM 隔离无法一手复核，归档时由 ENV 留档补全一手链）。
2. **起飞晚于清场完成**：06:57 起飞 vs 06:52 净空实测（T25 F5），无 06:24 趟 F4 式清场重叠违序。
3. **自然收轮三证可归档**：`RUN3_EXIT=1` + `1 failed` + session 自消（非挂死非中断）。

### 2.2 标签与计数

- **标签登记为 run4**（T25 §2.2 顺延裁决）。ENV 复用 `run3` 字面 + 覆写根级旧 log = **命名轻微违规，不构成降级**——§3.3 降级锚点仅「缺真空档」与「清场重叠」，两项本趟均满足；旧 log 内容已有 `run3-diagnostic-0624/` 归档副本，覆写可容忍。看板须补双向消歧行：**06:24 趟 = run3-diagnostic-0624（诊断）；06:57 趟 = run4（决定性 leg1）**。
- **×2 计数器 = 0/2 不变**（✘ 腿不计通过、不折半计 0.5）；新增登记「决定性 ✘ ×1」。诊断面 tally = **2✓/2✘**（run1/run2 ✓ + 06:24 诊断 ✘ + run4 决定性 ✘）——恰落入 T25 §2.4 预案的 leg1-✘ 分支。

### 2.3 判读 B：**触发**

git 一手铁证：`(26,-8)` 仅存在于 pr-129-head（`89d1cbb`）谱面——WP-A「出泊右转南下」候选线第一途径点（main 谱面零匹配）→ **run4 必然跑在 #129 候选线上**。候选线自身第一途径点腿失败 = **候选线边际缺陷坐实**，比 T25 §2.4 预设的「驶回腿复现」判据更直接。即刻生效三项：

1. **HOLD leg2**——禁再开决定性趟；
2. **#129 合流窗（Tick#31–32 顺延窗）作废**，#129 维持 draft 双门原状（当前候选线上 ×2 ✓✓ 已不可能凑齐）；
3. 按 #143 §3.4 启动**判读 B 回炉三候选**（含 Tick#13 出土 `a59d1ea` B 案走廊证据链对表）——注意这是**第二次判读 B**（#129 本身即 T12 #126 判读 B 的兑现段），纯改线方法论已两败，几何/口径候选权重上调（见 §3）。

---

## 3. 裁决 B：失败归因与下一棒

### 3.1 归因：#129 修复不足实锤（改线维度），但卡点族须消歧

- **同族确认，但族名更正**：run4 卡点 `(27.4,-21.8)` 与 main@`88097f9` 对照跑卡点 `(25.2,-25.7)`（#134 报告一手：04:14–04:26，500s 挪 1.4m 纯爬行）同域——**「出泊区东侧爬行族」（x∈25~28, z∈-21~-26）**，而非「桥区族」：X2 桥腿 `(±15.7,-26)` 距卡点 12m+，且 pr-129 构建（base `88097f9`）**根本不含 X2 几何**。看板登记用「出泊区东侧」，勿写「桥区」。
- **BL1 减深与本败无因果**：卡点 z=-21.8 在 BL1 桩带北界 z=-26 之外，车未及桩带。#129 的「改线」成功绕开了桩带，但没绕开出泊区爬行源——**改线绕不开出发点**。
- **待 triage 消歧的两个主假设**：① 出泊区东侧存在几何擦挂/未在册碰撞面（几何缺陷）；② SwiftShader 低帧（#134 实测 0.699–0.778 fps）下控制环退化，低速爬行超时（口径/预算缺陷）。06:24 趟失败瞬间车速 27 km/h（移动中超时）倾向 ②，main 对照跑 500s 挪 1.4m 倾向 ①——两趟签名不同，**必须 trace 双趟对比后再定**。

### 3.2 下一实现动作排序

| 序 | 动作 | 口径 |
|----|------|------|
| ① | **triage 摘要**（必做前置，零跑道） | run4 `test-results/trace.zip` ⊕ `run3-diagnostic-0624/fail-dir/trace.zip` 双趟对比：legA 轨迹 + 速度序列，分辨「几何擦挂爬行」vs「低帧控制退化」。**证据文件域只在 ENV VM → triage 只能由 ENV 随归档产出**（纯文本摘要入 evidence 目录），不可新派独立 Task（新 VM 拿不到文件） |
| ② | **出泊区几何专项**（若 triage 判几何） | 先评估 #134 域覆盖：其修复（桥位 z -26→-19.5 + 东北簇 `(17.8,-17.8)` 内退）**均不覆盖** `(25~28,-21~-26)` → 大概率需新开定向补洞段「出泊东区碰撞面减深/内退」，进回炉三候选评估 |
| ③ | **改线微调**（WP-A 前加过渡点 / radius 放宽） | 仅作几何修复配套，**不单独再烧决定性趟**（纯改线已两败） |
| ④ | **BL1 加深减深：剔除** | 无因果实证（车未及桩带） |

若 triage 判低帧控制退化 → 升级为 e2e 口径/性能预算议题（#134 报告「预算贴地」归因对表），同入回炉三候选。

### 3.3 是否允许 ENV 立即再开下一真空趟：**否**

HOLD leg2（T25 §2.4 预案分支）。修复未落地前重跑必复现，烧 ~20 min 跑道零信息增量。ENV 本 tick 唯一动作 = **归档 + triage 摘要**：`run4-decisive-0657/` 标签（或同义消歧名）+ **worktree HEAD SHA 登记**（预期 = `89d1cbb` 或其派生，归档必录以封死基线争议）+ 三证 + 真空档 token 行过滤（T25 §2.3-2）。

---

## 4. 裁决 C：扇出 / 禁扇出清单（本 tick）

### 4.1 可派 / 可动

| # | 项 | 口径 |
|---|----|------|
| 1 | **ENV 非中断排队催办 ×1**（唯一对 ENV 动作，**非 resume**） | 三合一内容：① run4 标签消歧归档（含 HEAD SHA + 三证 + token 过滤）；② HOLD leg2、禁自行再开决定性趟；③ 产出 triage 摘要（§3.2-①）。排队送达，不打断在飞归档 |
| 2 | **指挥官授权催办包 ×1** | **#146 催授权合入**（CI 绿 + ready + MERGEABLE 本单 gh 一手复验；合入权在指挥官，父代理禁代合）+ **存档波第一波 #105/#107/#110/#111/#113** 同包一次请授权。**#146 合前功能维持续读 84，禁冒登 87** |
| 3 | 秘书 3n 单（到点则发） | 登记：run4 决定性 ✘ + 判读 B 触发上板 + 违纪账（06:24 趟跳门 +1 级，T25 §5-6 尾账）+ run3/run4 双向消歧行 |

### 4.2 禁派 / 禁动

| # | 项 | 依据 |
|---|----|------|
| 1 | 新决定性趟 / 任何 chrome 级活动 | HOLD leg2 + #143 §3.5 互斥硬令 |
| 2 | fps-probe / lighthouse / playwright | 董事会永久禁并发令 + 本单硬纪律 |
| 3 | 独立 triage Task | 文件域不可达（证据在 ENV VM，新 VM 空盘），见 §3.2-① |
| 4 | 回炉三候选实现段 | 等 triage 归因落地再开，防歪打（几何 vs 口径未定） |
| 5 | #134 ready / #104 ready / #43 合入 | 冻结待段末审计 / NO-GO 红线不变（#134 转为回炉候选评估输入，仍不 ready 不合） |
| 6 | 顾问链新扇出 | 本事件已消费；下一合法事件 = triage 摘要落盘 / 指挥官授权动作 / 回炉裁决僵持 |

**resume ENV：否**——面板 RUNNING（07:13Z 实测）疑在归档中，resume 即抢跑打断；仅发 §4.1-1 非中断排队催办。

---

## 5. 裁决 D：董事会触发 = **NO-GO**

| 条件 | 本 tick 实况 | 判 |
|------|-------------|----|
| ① 连续无有效新增 | 反例充分：run4 出首个**决定性**失败样本（含合规真空档）+ ENV 自我纠正三查脚本（T25 §2.3-1 落实）+ #146 CI 绿备好合入 | 不满足 |
| ② 角色冲突 | ENV 规程自洽（真空档修复自执行、按序起飞）；无互污事件 | 不满足 |
| ③ 卡门 | 判读 B 触发是 §3.4/T25 §2.4 **设计内分支**，下一步明确（triage → 回炉三候选），非无路可走 | 不满足 |

**复评条件（任一满足即开庭）**：a) 回炉三候选裁决出现角色僵持，或连续 2 tick 无人接棒；b) run4 归档揭示 worktree HEAD SHA 不在 pr-129 域（`89d1cbb` 及派生之外）——归因基线错误级流程事故；c) #146 + 存档波授权包连续 2 tick 无指挥官响应（执行产能空转）。

---

## 6. 父代理即时清单（≤6 条）

1. **不 resume ENV**；发 §4.1-1 三合一非中断排队催办（归档标签 + HOLD leg2 + triage 摘要）；
2. **#146 + 存档波第一波打包催指挥官授权**；拿到授权前功能维读 84、存档波不动；
3. 看板落：run4 决定性 ✘、×2 = 0/2、判读 B 触发、#129 合流窗作废、卡点族名「出泊区东侧」；
4. triage 摘要落盘后的下一事件窗再裁回炉三候选（几何专项 vs 口径/预算 vs 改线配套，排序按 §3.2）；
5. 违纪账尾账（06:24 跳门 +1 级）随秘书 3n 单落；
6. 全 VM chrome 静默维持（无在飞趟亦不解禁，直至回炉裁决放行下一决定性趟对）。

---

*本文档为 CC-LOOP-ADVISOR-T26 交付物（事件驱动单）；看板登记以 `docs/research/cyber-city-score-loop-orchestration.md` 单源为准，本单不改看板、不冒登分数。*
