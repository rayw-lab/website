| document | cc-vis-x2-obs-r2-diagnosis |
|---|---|
| status | DIAGNOSED（P1 定向归因正本） |
| date | 2026-09-02 Asia/Shanghai |
| candidate | PR #104 head `598764172250f3a0d6e5a29c36aa564dbd44e009`（R2 exact head，未吸收 main 前原样复现） |
| worktree | `/private/tmp/x2-obs-r2-diag-20260902`（detached @ `5987641` + 本地诊断提交 `2596279`/`c837ff8`，仅插桩与对照腿，不进 #104） |
| environment | Node v22.23.0 + pnpm 10.33.3；port 4593（预 bind 正证据 + HTTP 200 + 后 bind 负证据）；workers=1 / retries=0 / 单 attempt；Playwright 1.62.1；`--no-deps`（诊断窗跳过依赖链，正式 R3 不适用） |
| inputs | 逐拍轨迹 JSON ×4（首轮）+ 对照走廊轨迹（次轮）、`session-dump-funnel.json`、Lane 3 静态几何独立复核 `cc-vis-x2-collider-aabb-20260902.md`（双盲） |

# CITY-OBS-01 R2 破门根因诊断（cc-vis-x2-obs-r2-diagnosis）

## 0. 结论一句话

**根因 = 测试路线缺陷（P2a 测试侧）**：`driveTo` 直瞄 bearing 从 `(20,-8)` 指向 `(28,-28)` 正穿 autodrive-lab 前院道具带（H11/H12 警示隔离墩 + 东北簇 S1/S2/S3，Q0/Q1 品质档下启用）；控制器无避障、楔死后恢复策略（同 bearing 重试 + respawn 回原点重走对角线）不改变进入几何 → 停滞循环。R2 的 360s 耗尽时恰处 respawn 出口 `(1.3,-2.1)`。**产品几何无缺陷、respawn 实现无缺陷、触发圈可达性无缺陷**——三者均被本轮证据排除。

## 1. 复现与环境事实

| 轮 | 用例 | 结果 | 关键数字 |
|---|---|---|---|
| 首轮 | CITY-OBS-01（插桩） | **PASS 8.4m**（R2 同 head 原样代码） | leg1 71.9s 干净；leg2 274.1s、**5 次 escape（2 倒车→R 重生→2 倒车）**、274s 才到 |
| 首轮 | CITY-PERF-02 | PASS 1.6m | leg2 11.9s 直达零楔死（`?quality=2` 深链 → 道具碰撞体从未创建） |
| 次轮 | CITY-OBS-01 + 对照走廊腿 | **PASS 10.4m** | leg2 直瞄基线重采样 **350.2s / 6 escapes / 2 次 R 重生**（楔死复现）；对照走廊三腿合计 86s / **0 escapes**（§5） |

- **R2 的 FAIL 形态在次轮基线腿获得近乎超时的复现**：同 exact head 下直瞄 leg2 两次采样均复刻「H12 楔死 → 倒车 → 再楔 → R 重生 → 对角线 S2 再楔 → …」循环；次轮 350.2/360s 到点，R2 的负载条件（72 例连跑后 fps 1-2、CDP 往返 5-10s）把同样的循环推过 360s 线。孤立 2 例窗 + 轻载 = 循环在预算内侥幸出圈（首轮 274s、次轮 350.2s），故两轮用例均 PASS——**用例 PASS ≠ 无缺陷，循环机制两轮均在档**。
- vacuum 登记：无 Playwright/chrome-headless/Astro 外部进程；2 个无关项目空闲 vite dev server（port 5174，CPU≈0，无浏览器）在册 informational；用户桌面 Chrome 属常态。

## 2. 楔死点与阻挡物逐帧对齐（首轮 leg2，75 帧；首轮原始 JSON 未存档——次轮 Playwright 清空 test-results，本节数字不可原始复核，机制由次轮重采样独立复现，见 §1/§5 与 P1 审计 P1-1 注记）

| # | 触发瞬间（轨迹 t） | 车体中心 | yaw | 速度崩塌 | nose/前角反推落点 | 命中道具（file:line 溯源） |
|---|---|---|---|---|---|---|
| 1 | t=37.9s | (24.52,-13.93) | 1.192 | 21.99→0.24 km/h | nose (25.11,-15.42) | **H12 警示隔离墩B** `[25.02,26.98]×[-15.94,-15.06]`（HeroBlenderMesh.ts:72） |
| 2 | t=84.7s | (25.27,-13.76) | 1.361 | 0.19→0.02（持续顶住） | nose (25.60,-15.32) | H12 同上 |
| 3 | t=127.4s（R 重生） | (25.51,-13.71) | 1.360 | 0.20→0.06 | nose (25.78,-15.29) | H12 同上 |
| 4 | t=193.8s | (15.98,-13.91) | 1.114 | 20.05→3.53→0.52 | 前角 (15.77,-15.78) | **S2 东北簇 Cabinet** `[15.63,17.28]×[-17.32,-15.59]`（StreetProps.ts:44,53-57,198-223） |
| 突破 | t≈250s | (15.55,-13.68)→(16.48,-15.65) | 直线 | 33.26→11.5 km/h | 掠过 S2 角 | cone-hit +6 @ 会话 444.9s——高速斜撞「犁」过，非走廊通行 |

- 楔死共性：**车体 nose（前探 1.6m 推土铲）或前角探入道具 AABB 后速度瞬时归零、满油顶死**（speed 0.01-0.6 km/h 持续 12s+ 触发 no-progress）。
- `R` 重生后（t=140.3s）**post_respawn = (0.000, 0.000, yaw 1.571)**——respawn 恒回城市 spawn 原点（见 §3 H2）；对角线重追 `(28,-28)` 在 x≈15.7-16.0 再次撞上 S2 簇（楔死 #4）——**同一障碍带、第二次**。
- 突破非能力证明：末段 (15.55,-13.68)→(16.48,-15.65) 以 33 km/h 掠过 S2 北西角（速度骤降 33→11.5 = 接触减速），后续 (17.28,-16.19)→(21.13,-18.77) 穿 S2/S3 间隙——属**斜撞+动量侥幸**，不可靠（R2 即耗尽在循环里）。

## 3. H1–H4 逐条判定

| # | 假说 | 判定 | 证据 |
|---|---|---|---|
| H1 | leg2 楔死 → 2 次倒车无效 → `R` 重生 → 从原点直追 → 循环直至超时 | **PROVEN**（带精化） | 轨迹完整复刻该循环：楔死×3 @ H12 → t=127.4 R 重生 → t=140.3 post_respawn (0,0) → 对角线重追 → 楔死 #4 @ S2；R2 终态 (1.3,-2.1) = respawn 出口滑移态（与本轮 t=142.7 帧 (0.22,-0.63) 同形态）。精化①：楔死不是「leg2 某处」而是特定道具带（H12/S2）；精化②：R2 超时不是 respawn 逻辑本身坏，而是「恢复后同 bearing 重试」永远再进同一停滞面，R2 的循环未获得本轮 t=239.5s 那次「宽位再进」机会。 |
| H2 | respawn 落点不是原点而是「最近路口」，yaw/位置错配 | **DISPROVEN** | ① 代码：`Player.respawn` → `getClosest(position)`，但注册表仅 1 项 `landing`（Game.ts:161 注册；city spawn 链 = `World.ts` `CITY_SPAWN` → `SPAWN` 常量，`index.ts:160-162` 覆写 landing 为 spawn (0,0) 朝北）——单元素注册表的 getClosest 恒返回原点；「已复位 · 回到最近路口」仅 DriveFeedback.ts:46 的 toast 文案，与实现不符。② 轨迹：post_respawn 帧逐字节 `(0.000, 0.000, yaw 1.571)`。③ driveTo 每拍重读 state()，无原点假设；重生后 bearing 重算正确（轨迹 t=142.7-175.2s 车正常驶向目标）。 |
| H3 | 泊车位触发圈被楼体裙房/道具簇挡住，入圈 Minkowski clearance < 0 | **DISPROVEN** | ① 本轮 OBS leg2 终态 `(27.57,-23.91)` dist=4.12 < 4.5 进入判定圈，PERF-02 终态 `(26.38,-26.68)` dist=2.09，双双触发 `poi-bounding-in` 并完成 E 进站。② Lane 3 静态：圈心 clearance 5.22m 全场近优，N/E/W 半盘全开，唯一死区在 SE 弧（楼体台阶咬入，本路线不经过）。阻挡物在圈外 z≈-14 道具带，不在圈内。 |
| H4 | 产品碰撞体（X2 立面/前景景框/StreetProps）在 #104 新增几何上与路带重叠 = 产品缺陷 | **DISPROVEN** | ① 楔死道具均为**设计摆放**的城市道具（警示隔离墩 A/B、东北簇 Vending/Cabinet/Bin），AABB 与源码 rotY 抖动算法逐一复刻吻合（Lane 3 全表 file:line 溯源），无错位/穿模/多余 collider。② 触发圈可达（§3-H3）；walkable 走廊存在（Lane 3 东弧 M=1.836 全程无碰、west-lane M=0.9；PERF-02 的 Q2 世界 + 本轮末段均实证入圈）。③ 本轮同 exact head 双例全过，产品侧无常驻缺陷面。产品 collider **不是**根因。 |

**新增 H5（路线先验不足）— PROVEN**：`(20,-8) → (28,-28)` 两点连线在 Q0/Q1 世界不可无碰行驶（Lane 3 §6：正穿 H11 隔离墩A `(22.72,-14.81)→(23.28,-16.19)`、垂足距 H10 备件箱 0.39m）；本轮轨迹实证直瞄控制律恰好停滞在 H12/S2 的鼻尖停滞面。**R1 时点 triage 注记的「桩排带墙 X1」在本 head 已收缩至 `z∈[-40.25,-29.75]`，不再是本失败面的阻挡物**（Lane 3 纪律发现：spec 头注与 `audit-x2-visibility.mjs:196` 常量过时；「SpeedTrap 隔板」系注释张冠李戴，SpeedTrap 从无碰撞体）。

## 4. 品质档位时序（为何 R2 挂而 PERF-02 不挂）

| 用例 | 品质路径 | 道具碰撞体 | 结果 |
|---|---|---|---|
| CITY-OBS-01（R2 与本轮同） | 挂载 Q0（GLB 加载、道具启用）→ SwiftShader fps 1-2 触发 auto-drop 0→1→2（**首轮 t=178.3s/437.5s（原始件未存档）；次轮存档 dump 实录 t=136.2s/369.4s**） | **启用**（`show = level < 2`，FacadeKit.ts:145-154） | 直瞄撞道具带 → 楔死循环 |
| CITY-PERF-02 | `?quality=2` 深链：挂载即 Q2（零 GLB 请求零解码，FacadeKit.ts:71-72） | **从未创建** | 直瞄无阻挡，11.9s 直达 |

`FacadeKit.registerBody`：`if (quality.level === 2) objects.disable(body)`（FacadeKit.ts:142-144）——Q2 无「隐形墙」合同已由源码证实；R2/R1 证据链中 OBS 与 PERF 的差异由此完全解释。

## 5. 对照实验（P1「对照」项：直瞄 vs 安全入圈点）——已回填

候选走廊取 Lane 3 东弧（M=1.836 全程无碰，瓶颈 `(33,-16)` 净距 1.84m = H9 冷却罐与 H7 升降台间隙）：`R 复位回原点 → leg1 (20,-8) r3 → 途径点 (33,-16) r2.5 → 泊车位 (28,-28) r4.5`。次轮诊断窗（CITY-OBS-01 全程 10.4m，`RUN_EXIT=0`，`[X2-DIAG] 对照走廊: leg1=true (33,-16)=true bay=true 终态 x=30.45 z=-26.84`）实测：

| 腿 | 耗时 | escapes | minDist | 终态 | 判定 |
|---|---|---|---|---|---|
| 基线 leg2 直瞄（同窗重采样 #2） | **350.2s** | **6**（2 倒车→R→对角再楔→2 倒车→R） | 4.38 | (25.46,-24.43) | 复现楔死循环：H12 `(24.51,-13.93)/(25.29,-13.76)/(25.64,-13.69)` → R → S2 `(16.07,-13.89)` → 2 倒车（后者 `(15.55,-12.64)`，位于 S2 AABB 北侧 ≈3m，簇间隙取向）→ R `(18.33,-15.17)`；350.2/360s 惊险到点 = **R2 超时形态实锤** |
| 对照 leg1 (20,-8) | 37.2s | 0 | 1.63 | (18.42,-7.59) | ✓ |
| 对照途径点 (33,-16) r2.5 | **26.6s** | **0** | 0.87 | (32.91,-16.87) | ✓ H9/H7 间隙无擦碰 |
| 对照 bay (28,-28) r4.5 | **22.2s** | **0** | 2.71 | (30.45,-26.84) | ✓ NE 弧入圈 → poi-bounding-in → E 进站全走 |

**对照结论**：同 exact head、同环境、同控制器，仅把进入几何从「直瞄」改为「东弧途径点」，leg2 从「6 escapes / 350s / 靠 2 次 respawn 侥幸」变为「0 escapes / 49s（两腿合计）/ 确定性到达」。机制差异单一变量 = 路线，P2 修法由此获得直接证据。

## 6. P2 最小修法建议（测试侧，待授权后执行）

- **修法**：`e2e/cyber-city-observability.spec.ts` 与 `e2e/cyber-city-perf.spec.ts` 同源同步——leg2 前插入中间途径点（候选 `(33,-16)` r2.5，东弧走廊；次轮对照数据为机制差异说明），把「直瞄穿越道具带」改为「绕东弧经 H9/H7 间隙南下入圈」。
- **零触**：timeout/radius 业务语义（判定圈仍 r4.5、触发圈 r6）、retry/skip/soft assertion、`playwright.config.ts`、`src/**`、视觉基线。
- **不做**：不改 respawn/恢复策略本身（路线改对后无楔死可恢复）；不给道具「挪位」（那是产品几何，且无缺陷证据）。
- 风险：Q2 世界（PERF-02）道具不存在，途径点纯绕行多 ~10s 预算内；东弧瓶颈 1.84m 对最坏外接半径 1.836m 为贴线值，若对照腿出现擦碰则回退 Lane 3 west-lane 候选（`(20,-17)` 中带，瓶颈 0.91m）或双途径点细化。

## 7. 局限与登记

- 本诊断基于 SwiftShader/webgl2 + Q2 自动降档环境（`env.quality` 终值 2、`quality-auto-drop` ×2 实录）；真机 GPU 档（Q0 恒定、道具恒启用）下直瞄同样会撞道具带——机制与品质档无关，仅时间窗不同。
- 插桩为零 CDP 往返旁路（本地对象 push + 一次落盘写），控制时序与 R2 同条件；插桩与对照腿仅存于本地诊断 worktree（提交 `2596279`/`c837ff8`），**不进 #104**。
- 次轮对照窗 vacuum/端口/单 attempt 纪律与首轮相同；两轮均为本地诊断窗，不构成 R3 授权，不覆盖 R2 NO_GO。
