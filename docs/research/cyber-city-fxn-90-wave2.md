# 功能 90 第二波清单（CC-FXN-NEXT-90 · wave2 决策树）

> 执行模型自报：**claude-fable-5-thinking-xhigh**

| 项 | 内容 |
|----|------|
| Task | **CC-FXN-NEXT-90**（顾问 Task · doc-only）——C5+C6 合流后的「下一刀」预案：若 AL-FXN-R5 登 ~87–88，给出真机 S-2 之外的代码实现清单；若已逼近 90，只列真机腿 |
| 分支 | `cursor/cc-fxn-next-90-1d6f`（base：`main` @ `771b1e4`，独立 worktree） |
| 日期 | 2026-08-27 |
| 性质 | 顾问/预案：**不改秤、不改门、不写代码**——功能秤恒为 `docs/spec/cyber-city-function-rubric.md` v1.0（S-2/S-5 v1.0）；本文件只做封顶复核、逐维决策规则与派单菜单；`src/`、e2e、config 零改动 |
| 输入 | `cyber-city-fxn-90-path-advisor.md`（84→90 顾问，封顶定理与杠杆表正本）· `loop8-fxn-audit.md`（84 首登 + §6 四缺口）· C5 已合事实（PR [#90](https://github.com/rayw-lab/website/pull/90) `b42405c`）· C6 已合事实（PR [#91](https://github.com/rayw-lab/website/pull/91) `5fc9533`）· R5 在途快照（`cursor/cc-al-fxn-r5-1d6f` @ `eeb78bc`，仅作校准，非终局） |
| 消费方 | 父代理（R5 落分后的第一个 tick 按 §2 决策规则判读情景 + 派单）· 指挥官（真机 S-2 窗口排期）· 可能的 wave2 实现 Task（W1–W5）· CC-AL-FXN-R6（下一登记轮） |
| 红线 | **AL-FXN-R5 并行取证中**：本篇及任何 wave2 后续动作在 R5 登记落地前不得触碰 `cyber-city-function-rubric-score.json` 与 `loop8-fxn-r5-audit.md`；本篇 §1/§4 的封顶向量与预测区间为诊断参考，禁止写入登记 JSON（rubric 禁止清单第 8 条） |

---

## 0. 结论先行

1. **本篇是 R5 落分前写好的决策树，不预判 R5 结论**：R5 无真机腿（其 kickoff 明示「F1/F2 计时高段锁 85，不伪造 90」），云端从严满配向量的合成封顶为 **87（F7 95）～88（F7 100）**（§1 算术）。因此 R5 的合法落点区间是 85–88；登 87–88 本身就是「C5/C6 交付面被审计全额确认」的信号，不是失败。
2. **封顶定理复核仍成立（核心裁决不变）**：90 路径顾问 §2.2 的结论在 C5/C6 合流后没有任何松动——F1「0:15」、F2「≤100ms」、F3「≤3s 恢复/主动要求继续开」、F4「30s 自然吸引」四个计时锚在 SwiftShader 禁令下云端恒锁 85。**不存在任何纯代码刀能把云端登记推到 90**；真机 S-2 腿的 +3.25 加权增量无代码替代物。
3. **决策规则（父代理判读用，一条即可）**：把 R5 逐维落分与 §1 云端封顶向量（F1 85 / F2 85 / F3 85 / F4 85 / F5 90 / F6 90 / F7 95+）逐格比对——**低于封顶的维才有代码刀可派（§3 菜单 W1–W5，逐维键控）；等于封顶的维只剩真机腿（§4 runbook）**。
4. **情景裁决**：
   - **情景 A**（R5 登 87–88 但存在低于封顶的维，或登 85–86）：按 §3 菜单只派被点名维的修补刀，合流后派完整审计轮 R6，真机腿与 R6 同拍；
   - **情景 B**（R5 登 87–88 且向量满配 = 已逼近 90）：**零代码刀**——再切任何代码都是净负收益（多一轮完整审计、云端最多 +0～1 分、90 依旧不可达）。只剩真机腿：指挥官 ≈15 分钟桌面 Chrome 窗口 + 轻量 R6 登记（§4）；
   - **情景 C**（R5 < 85 或某维回归性塌方）：不预设修法，按 R5 审计报告的缺口清单重新派单（本篇菜单作候选参考）。
5. **一条时序纪律**：wave2 任何改 `src/` 的刀都必须等 R5 登记 PR 合流后才开工——R5 评分对象已钉 `main@dc3f56b`，抢跑合流虽不污染其登记，但会使「登记 tip ≠ main tip」的窗口无谓拉长，且情景判读本来就依赖 R5 逐维落分。

---

## 1. 事实底座与云端封顶算术

### 1.1 C5/C6 已合事实（一手核验，main@`771b1e4`）

| 刀 | 合入 | 交付面 | 对应杠杆（顾问 §2.1） |
|----|------|--------|----------------------|
| CC-FXN-C5-R3 | PR [#90](https://github.com/rayw-lab/website/pull/90) `b42405c` | `QuestLine.ts`（426 行）：5 站主链「下一站」chip（楼名+距离+n/5，可折叠非强制）+ parkingBay 光柱（`NeonMaterials` 静态发光，不占 CITY-03 席）+ 链推进（boundingIn 消费）+ **`idle-30s` nudge 消费**；OBS 加法 `world-quest` 族；e2e CITY-QST-01/02 | B（F6 75→90）· C（F3 75→85）· E 的代码半 |
| CC-FXN-C6 | PR [#91](https://github.com/rayw-lab/website/pull/91) `5fc9533` | `DriveFeedback` 刹车徽标（双沿即按即亮）+ 悬挂跳 chip；`SpeedTrap.ts`（261 行）：实时 km/h · 驶离最高速 · SPEED DEMON · 慢车吐槽；埋点 `brake-first`/`suspension-jump`/`world-speedtrap`；e2e FB-07/08/09 | F 的代码半（F2 最后半价项清零）· D 的代码半（F3 玩点） |

R5 在途快照（`eeb78bc`，校准用非终局）已实证：S-2 主腿 chip 随 car_ready 激活、BRAKE 徽标双沿、悬挂跳 chip、V/R/E 三段闭环、三问原文直接引用「下一站」chip；L1 深链链推进 2/5；L2 三路径恢复 + fall 专属 toast；L3 ESC 双出口。**L4–L7（含决定 F5/F6 段位的 L6/L7）尚未回填。**

### 1.2 云端封顶向量（R5 无真机的从严满配）

| 维 | 权重 | 云端封顶 | 封顶原因 |
|----|:---:|:---:|----------|
| F1 首幕可懂 | .15 | **85** | 90 段锚「0:15 前说出下一步」是计时判定，SwiftShader 禁令锁 85（首登判例） |
| F2 操作反馈 | .20 | **85** | 90 段锚「≤100ms 可感知延迟」同上；C6 已把非计时半价项清零，但计时子句仍锁段 |
| F3 驾驶乐趣 | .15 | **85** | 90 段锚含「≤3s 恢复」「主动要求继续开」计时/真人观察子句；C5 目标线消灭寻路费力 + C6 测速牌给「想再来一次」玩点后，非计时部分已齐 |
| F4 POI 游戏化 | .15 | **85** | 90 段锚「光圈 30s 内自然吸引」计时子句锁段；深链/前奏/落点/楼=分区可转述已成立 |
| F5 人性化 | .15 | **90** | 无计时子句。90 需五腿全过 + **L6 单会话 Q2 七步闭合**（纯取证，零代码——R5 职责内） |
| F6 目标/进度 | .10 | **90** | 无计时子句（`idle-30s` 按设计秒判存在性/顺序性合法）。90 需 L7 实证 nudge 玩家可感 |
| F7 可观测完备 | .10 | **95～100** | 95 已是首登事实；100 属审计自由裁量（三通道+CI 工件全通），无明确代码欠账 |

**合成**：`85×.15 + 85×.20 + 85×.15 + 85×.15 + 90×.15 + 90×.10 + 95×.10 = 87.25 → 87`；F7 拉满 100 时 87.75 → **88**。若 L6 未闭合（F5 停 85）：86.5 → 87；若 L7 再失守（F6 停 85）：86.0 → 86。

**真机腿的算术（不变量）**：在满配 87 基础上，一条真机 S-2 腿同时解锁 F1/F2/F3/F4 四维计时高段 → `87.25 + 0.75 + 1.00 + 0.75 + 0.75 = 90.5 → 登记 90–91`。冗余：容 F5 或 F6 任一维滑落 85 仍 ≥ 89.75 → 90；两维同时滑落则 89.25 → 89，须回 §3 菜单补刀。

---

## 2. 决策规则（父代理在 R5 登记 PR 合流后的第一个 tick 执行）

```
读 R5 登记 JSON 逐维向量 →
  逐格比对 §1.2 封顶向量：
    存在低于封顶的维？
      是 → 情景 A：按 §3 菜单派被点名维的刀（只派点名维，禁扩批）
      否 → 情景 B：零代码刀，走 §4 真机腿 runbook
  总分 < 85 或出现 ≤70 维 → 情景 C：按 R5 报告缺口清单另行派单
  总分 ≥ 89（无真机而超云端封顶）→ 异常：触发登记有效性复核（rubric §6.3），不派实现刀
```

判读要点：**看维不看总分**。87 有两种成因——「F5/F6 已满配 + F7 95」与「F5 停 85 + F7 100」总分同为 87，但前者零代码刀、后者可能有一条 W3。逐维比对才能区分。

---

## 3. 情景 A：存在低于封顶的维——wave2 代码刀菜单（逐维键控）

> 使用规则：**每条 W 刀只在其触发条件被 R5 报告明文点名时才派**；未点名 = 不派。全部继承 C5/C6 硬门：`robot_idle` poster 逐字节恒等、CITY-03 循环配额、四降级轨声明（RM/触屏/Q2/KinematicFallback）、新事件 OBS 白名单加法、e2e 不降且新增断言、不动 `Player.ts` 键位表语义。真机 S-2 腿不在本菜单内（它是 §4 的事，且与本菜单不互斥——真机腿在任何情景下都是 90 的必要条件）。

| # | 触发条件（R5 报告关键词） | 刀内容 | 文件域 | 加权增量 | 备注 |
|---|--------------------------|--------|--------|:---:|------|
| W1 | **F6 < 90**：L7 判「nudge 不可感」——如 idle 时仅 chip 微动、chip 折叠态无 nudge、链完成后自由探索态无空闲响应、wandering 态不触发 | nudge 形态增强：idle 触发时 hint 再现 + 目标光柱脉冲（RM 轨改瞬时提亮）+ status 行「下一站」文案三选一升级为组合拳；补 chip 折叠态与 chain-complete 态的 nudge 分支 | `src/lab/world/areas/QuestLine.ts` · `src/lab/world/index.ts`（idle 接线）· `e2e/cyber-city-explore.spec.ts` | +0.5～1.5 | C5 已建骨架，属参数/分支级修补，非新子系统 |
| W2 | **F3 < 85**：Pass A 仍报「为找圈复位绕行/寻路费力」 | chip 增方位指示（相对车头的箭头或方位角）；首站选择改「出生点最近未访 POI」；光柱高度/亮度按出生视野校准 | `QuestLine.ts` · `src/data/world-pois.json`（链序）· `NeonMaterials.ts` | +0.75～1.5 | R5 在途三问已直接引用 chip 指路，本刀被触发概率低 |
| W3 | **F5 < 90 且缺口定性为 Q2 代码性阻塞**：L6 七步在 `?quality=2` 下存在功能缺失（如 chip/光柱/进站前奏在 Q2 轨缺席或塌陷） | 修 Q2 降级轨缺陷（以 R5 定位为准） | 视缺陷定位 | +0.75 | **辨析**：若 L6 只是「取证未在单会话闭合」（环境/耗时原因）→ 零代码，R6 纯取证重跑即可，禁派本刀 |
| W4 | **F2 有非计时残余半价项**：审计点名 S-2 动线内某输入仍无专属确认层 | 按点名补确认层（`DriveFeedback` 徽标/chip/toast 加法，与 boost/brake 同构） | `src/lab/world/world/DriveFeedback.ts` · `e2e/cyber-city-feedback.spec.ts` | ≤+1.0 | R5 在途已证 brake/悬挂跳/V/R/E/降档 toast 全闭环，碰撞确认由 e2e FB-06 灰盒承接——本刀大概率空集 |
| W5 | **F1 或 F5 非计时残余**：深链会话 Reveal 键位卡不挂载 → H 召回不可用被升格为缺陷（R5 在途暂裁「边界事实非缺陷」） | 深链会话补挂键位 hint（或 status 行广告「按 H 重看键位」同款） | `src/lab/world/world/Reveal.ts` · `src/lab/world/index.ts` | ≤+0.75 | 低优先级：脚本主腿（ritual 会话）hint 链已闭环 |

**菜单外恒禁（继承顾问 §5 禁扩批）**：G7 锥桶挑战、G3 音效层、G5 护照、G13 成就、G8 计时圈仍不在 90 关键路径上，任何 wave2 刀不得搭车。

**成本核算（派单前必读）**：任何 W 刀合流即产生新 tip → 必须重派**完整**审计轮 R6（双 Pass + 全腿，证据同 commit），成本 ≈ 一整轮登记。因此：
- 触发维的加权缺口 < 1.0 时，优先考虑「不切刀、直接走 §4 真机腿」——真机 +3.25 的冗余足以吸收单个 10% 维停 85（89.75 → 90）；
- 只有当 **F5 与 F6 双双低于封顶**（冗余耗尽，真机后 89.25 → 89）或 F3 < 85（15% 维低段，真机后 89.75 边缘）时，wave2 刀才是 90 的必要动作。

---

## 4. 情景 B：向量满配（已逼近 90）——只剩真机腿

**判定**：R5 登 87–88 且 F5 = 90、F6 = 90（F1–F4 恒 85 属计时锁，非缺口）。此时全站不存在任何有正收益的代码刀，唯一剩余动作：

### 4.1 真机 S-2 复测 runbook（指挥官人肉腿，非 Task）

| 项 | 内容 |
|----|------|
| 执行人 | 指挥官（唯一人肉腿；供证据不打分，判定归 R6 审计） |
| 对象 | **R5 登记 subject 同一 commit**（`main@dc3f56b`；若届时 main 前进但 `src/` 无变更，钉登记 commit 本身） |
| 设备/耗时 | 桌面 Chrome，≈15 分钟；无安卓依赖（安卓是性能轨的事） |
| 动作 | S-2 v1.0 一遍（首访清存储）+ 全程录屏 + 每腿 `JSON.stringify(window.__worldSession.dump())` 落盘 + 三问原文逐字记录 |
| 四个计时锚 | ① F1「0:15 前说出下一步」；② F2「≤100ms 可感知延迟」（逐输入体感）；③ F3「翻车/卡墙 ≤3s 恢复」+「60s 结束时主动要求继续开」；④ F4「光圈 30s 内自然吸引进站」 |
| 归档 | 录屏 + dump + 三问三件套入 `docs/spec/assets/`（命名含日期与腿别，human-gate §2.0 三件套纪律同构） |

### 4.2 登记轮 CC-AL-FXN-R6（轻量）

- **复用 R5 全部非计时证据**（同 commit 前提下合法——顾问 §4 风险预案的既定安排）；
- 只补四个计时锚的复核与逐维改分（F1/F2/F3/F4 视真机证据 85→90）；
- 重写 `cyber-city-function-rubric-score.json`（只改分值与证据，schema/秤不动）+ 轻量审计报告；
- 有效性照走 rubric §6.3 五条：同 commit 全量 e2e 绿、分差 ≥±10 维写差异说明、`scoredBy` 独立署名、实现方自评永不登记。

**算术出口**：87.25 + 3.25 = 90.5 → 登记 **90–91**；容 F5 或 F6 任一维 85 仍 → 90。真机窗口若仍缺席，功能轨在 87–88 诚实驻留，90 无限期顺延——**这是设备约束不是工程欠账**，禁止用任何云端手段伪造计时证据。

---

## 5. 禁止清单（对本篇消费方与所有 wave2 后续 Task 的约束）

1. **禁触在途登记**：R5 落笔前，任何人不得改 `cyber-city-function-rubric-score.json` 与 `loop8-fxn-r5-audit.md`；本篇封顶向量/预测区间禁入登记 JSON。
2. **禁抢跑**：wave2 一切改 `src/` 的刀等 R5 登记 PR 合流后开工（§0-5 时序纪律）。
3. **禁扩批**：只做 R5 点名维的最小修补；G7/G3/G5/G13/G8 不得搭车。
4. **禁改秤**：rubric v1.0 / S-2 v1.0 / S-5 v1.0 恒定；OBS 只做白名单加法；禁为凑 90 调权重、改锚点、增删腿。
5. **禁自评登记**：W 刀实现方不得触碰登记 JSON 与审计报告；唯一登记出口 = CC-AL-FXN-R6。
6. **禁伪造计时**：SwiftShader 时长/帧率永不作计时判定；真机证据产不出就留空列欠账。

## 6. 派单建议与看板行

| 时机 | 动作 |
|------|------|
| R5 登记 PR 合流后第一个 tick | 父代理按 §2 决策规则判读情景；同 tick 无论情景 A/B，**都向指挥官发出真机 S-2 窗口请求**（90 的必要条件，与代码刀不互斥） |
| 情景 A | 派被点名维的 W 刀（并行性：W1/W2 同域 `QuestLine.ts` 必须同单或串行；W4/W5 与之无交集可并行）→ 合流 → 派完整轮 CC-AL-FXN-R6，真机腿同拍 |
| 情景 B | 零代码刀；真机窗口就绪后直接派轻量 CC-AL-FXN-R6 |
| 情景 C | 按 R5 报告另行派单，本篇菜单作候选参考 |

**看板在途行（供父代理粘贴）**：

```
| CC-FXN-NEXT-90 | Fable5 xhigh | ✅ wave2 决策树已备（等 R5 落分判读情景 A/B） |
| 真机 S-2 窗口 | 指挥官 | ⏳ R5 落分后即请排期（≈15min 桌面 Chrome，90 必要条件） |
| CC-AL-FXN-R6 | Fable5 xhigh | ⏸ 待情景判读（A=完整轮 / B=轻量轮） |
```

功能轨「下一拍」建议改写为：`R5 落分 → §2 判读（A: W刀→R6完整轮 / B: 真机→R6轻量轮）→ 90 判定登记`。

---

*CC-FXN-NEXT-90 · 2026-08-27 — C5（#90 目标线+空闲引导）+ C6（#91 F2 确认层+测速牌）已合流后的 wave2 决策树：云端从严满配封顶 87–88，纯代码刀无一能达 90；R5 登 87–88 且向量满配 = 零代码刀只剩真机腿（+3.25 → 90.5）；向量有洞才按 W1–W5 逐维键控补刀，且单洞 <1.0 加权时真机冗余可直接吸收、不值得多开一轮审计。doc-only，`src/` 零改动，登记 JSON 零触碰。*
