# CC-LOOP-ADVISOR-T7 Tick#7 扇出裁决（fanout）

- **顾问**：CC-LOOP-ADVISOR-T7（model slug: `claude-fable-5-thinking-xhigh`）
- **时间**：2026-08-28 03:40–03:45 UTC（Tick#7）
- **基线**：main @ `88097f9`（本 tick `git fetch` 实测，无新合入）
- **纪律**：零 src/ 改动，本文档为唯一交付物；产出于独立 worktree `/tmp/t7-wt`
  （base = origin/main），未触碰 `/workspace` 共享检出、未杀任何进程、未代任何一路
  push。登记看板单源仍为 `docs/research/cyber-city-score-loop-orchestration.md`。

---

## 0. 事实核查（fresh 取证，全部本 tick 03:41–03:43 实测）

| 对象 | 实测状态 | 取证方式 |
|------|---------|---------|
| main | `88097f9`，无新合入 | `git fetch` + `rev-parse origin/main` |
| **#103 FXN-R7** | **tip 已推进：`c4e844c` → `1a4296f`（03:40:08 UTC）**——Codex 已 push，父代理 tick 简报的「仍未 push」已被事件超越 | `git fetch refs/pull/103/head` + `gh pr view 103` commits 实测 |
| #103 push 内容 | `1a4296f` = CC-FXN-R7-CODEX-CLEAR 三项清账，13 文件：审计报告 + score JSON + **8 PNG + 8x mp4（3.9MB）+ session dump + capture summary 全部入库** `docs/research/assets/fxn-audit/`；分值零变动 87 | `git log --stat c4e844c..1a4296f` |
| #103 去 ephemeral 化 | **预检通过**：score JSON 零 `/tmp` 引用；正本证据全指仓内路径；66MB 原始 webm 未入库（仅 3.9MB 8x mp4 入库，取舍正确）；报告中残留 `/tmp/wt-*` 字样均为世系叙事（worktree 路径记录），非证据指针 | `git show 1a4296f:… \| rg /tmp` 实测 |
| Codex worktree | `/tmp/wt-fxn-r7-codex` 与 `origin/cursor/cc-al-fxn-r7-1d6f` **零 ahead 零 dirty**——打包动作已完成 | `git status -sb` 实测 |
| #103 门禁 | 新 tip `1a4296f` 门禁 **IN_PROGRESS**（合并前置） | `gh pr view 103 statusCheckRollup` |
| **X2 独占复跑** | **进行中**：`pnpm test:e2e` PID 20226 于 ~03:39:29 起（etime 反推），astro build → playwright 双 worker 在飞，chromium SwiftShader GPU 进程 311% CPU 独享 | `ps -o pid,etime` + 全进程扫描 |
| X2 窗口独占性 | **成立**：capture run3 DONE 03:32:30（run3.log「video saved」+「DONE」），mp4 转档 03:34 完成；e2e 03:39:29 起跑——**零重叠，间隔 ≥5min**；本 tick 全进程扫描无 capture-l6r/ffmpeg 存量 | run3.log + out/ mtime + ps 三方互证 |
| X2 test-results | 空（新轮清场，~3min 处尚无失败落盘） | `ls test-results/` |
| X2 worktree 脏区 | `docs/spec/assets/e2e-batch1/*.png` 多张被测试重写（M 状态）——**收口提交前必须还原**（历史截图纪律） | `git status -sb` 实测 |
| #104 X2 | draft，tip `c24c7f3` 静止，门禁在现 tip SUCCESS | `gh pr view 104` |
| #112 / #116 | 均 draft OPEN，**仍待指挥官人工合入**，无变化 | `gh pr view 112/116` |
| VM 负载 | **4 核，load 4.17 / 3.56 / 5.87**（1/5/15min）——5min 均值自 T6 的 9.50 **降 62%**；当前 ~4.2 即单路全量 e2e 的正常水位 | `uptime` + `nproc` |
| /workspace 残留 | 仍挂 T2 污染 ref（ahead 4），T3 §4.2 处置未执行；维持 T4 §4 原判，不在本 tick 动 | `git status -sb` |

---

## 1. 本 tick 父代理动作：**双等改单等 + 一条门禁跟进，零打扰**

任务简报默认动作（等 Codex push + 等 X2 结局）中，**前半已在取证中被证实完成**。
本 tick 动作收敛为：

1. **#103 门禁跟进（唯一新增动作）**：`1a4296f` 门禁 IN_PROGRESS。绿 → 报指挥官
   人工合入 #103（连锁：合入后功能登记 87 走秘书线，T4 §5 口径）；红 → 传话 Codex
   修复（docs-only 面，大概率 links/budget 类，无需重载）。父代理不代合、不代修。
2. **等 X2 独占 e2e 结局，全程零打扰**：PID 20226 预计 03:57–04:03 自然收轮
   （17–23min 全量口径，03:39:29 起跑）。**结局前禁止在本 VM 开任何新重载**
   （见 §5 窗口保护条款）。
3. **不 resume 任何一路**：Codex 交付已落地（§2），X2 实时活跃——两路都无 resume 事由。
4. #112 / #116 维持指挥官人工窗口，父代理只传话不代合。

---

## 2. Codex DONE→push 窗口裁决：**已被事件超越——push 于 03:40:08 落地，无需催**

### 2.1 结论

**不催、不 resume。** 简报取证（03:40）与本顾问取证（03:41）之间，Codex 完成了
push：`1a4296f` 已在远程 #103，worktree 零 ahead 零 dirty，打包-推送链全部走完。
DONE（03:32:30）→ push（03:40:08）实际窗口 **7.6 分钟**，其中含 mp4 转档（03:34）、
commit 组装（13 文件 291 insertions）——节奏正常，事后看任何催促都会是噪音。

### 2.2 残余判定阈值（Codex 的 resume 时钟就此退役，换门禁时钟）

交付物已入远程，「进程活性」不再是 Codex 线的监控对象。新时钟：

| 检查点 | 阈值 | 动作 |
|--------|------|------|
| 门禁出结果 | `1a4296f` 提交于 03:40，门禁常态 8–15min → **03:55 前应出** | 绿 → §1-1 合入连锁；红 → 传话修复 |
| 门禁悬挂 | **04:00 仍 IN_PROGRESS**（>20min）| 查 workflow run 是否排队/卡死，属 CI 基建问题非 Codex 问题 |
| Codex 代理 RUNNING 残留 | 代理可能在收尾（PR body/看板注记）。**唯一破窗风险**：若其自发再起 run4 补拍会撞 X2 独占窗口 | 无需干预其收尾；但 Tick#8 任务书写明：如 Codex 线还想补拍，一律排 PID 20226 退出之后 |

### 2.3 Tick#8 落库检查预填（本 tick 已预检 70%）

去 ephemeral 化实测通过（§0 第 4 行）。Tick#8 只剩两项：① 门禁对新入库 ~7.5MB
二进制（8 PNG + mp4）的 budget 判定——budget 门为页面传输体积口径，docs 资产
不计入，预期绿，但以门禁实际结果为准；② 报告内锚点链接抽查（图片相对路径在
GitHub 渲染是否可达）。

---

## 3. X2 独占复跑：验收细则 + T7-A 视觉审计入场时点

### 3.1 本轮复跑的证据地位

本轮（PID 20226，03:39:29 起）**满足 T6 §2.2-1 独占窗口硬门**：起跑时间戳 vs
capture DONE 时间戳零重叠 + 本 tick ps 快照无并发重载 + load 单路水位。三方证据
已在 §0 固化，X2 收口报告可直接引用本文档，不必重新自证。

### 3.2 分支裁决树（按本轮结局三分）

| 结局 | CITY-EXP-01（T6 案 #1）验收 | 崩溃簇（T6 案 #2–#5）验收 |
|------|------------------------------|--------------------------|
| **52/52 全绿** | 「独占窗口复跑通过 → flake 定性」收案：竞争下物理步进失真坐实，R2 碰撞体回归嫌疑排除，**无需改码** | 环境定性坐实（`Channel closed` = 共载崩溃 + 独占轮零复现的正证），打包收案，引 T6 §2.1 证据链 |
| **仅 CITY-EXP-01 复挂**（语义断言复现：车未达泊车位） | **真回归嫌疑坐实** → 开定向补洞单：R2 立面套件/前景景框 collider 域排查（泊车位 (-28,-28) 进路沿线），修复 commit + 绿门禁后才可 undraft。门控串行，不降门不硬闯 | 若 #2–#5 独占轮零复现，仍按环境定性收案，与 #1 补洞解耦 |
| **独占轮仍现 `Channel closed`** | 按左列各自判 | **环境定性动摇**：无共载可赖 → 须查 worker 崩溃 dump / 内存水位，升级为环境专项，T7-A 入场顺延 |

**通杀条款（三分支共用）**：收口提交前必须还原被测试重写的
`docs/spec/assets/e2e-batch1/*.png`（本 tick 实测已有多张 M 状态）；审计必查
`git diff --stat` 中不得混入截图噪声。

### 3.3 T7-A 视觉审计入场条件与时点

入场五条件（全部满足才开单）：① 本轮 52/52 或补洞后复跑 52/52；② 5 例处置
报告落 #104（按 §3.2 对应分支写）；③ 历史截图还原；④ #104 undraft + fresh 绿
门禁；⑤ 入场时 X2 线 IDLE（审计与实现不同时在飞）。

时点推演：run 收轮 03:57–04:03 → 全绿路径：处置报告 + 截图还原 + push + 门禁
重跑 ≈ 15–25min → **ready 最早 ~04:20–04:30，T7-A 入场最早 Tick#10**（与 T6
§2.3 预估吻合）；补洞路径：collider 排查 + 修复 + 独占复跑再一轮 ≥ 05:00，
入场顺延至 Tick#13+。T7-A 审计清单沿用 T5 §4 原文 + T6 §2.2-3 增补（独占性
核验 + 5 例逐类处置核验，缺一打回）。

---

## 4. Tick#8 预排（~03:50 UTC）

### 4.1 阈值档更新

| 路 | 活性锚（本 tick 刷新） | 状态 | 下一检查点 |
|----|----------------------|------|-----------|
| Codex | 交付已 push（03:40:08） | **resume 时钟退役** | 门禁时钟：03:55 应绿 / 04:00 悬挂查 CI（§2.2） |
| X2 | e2e 进程活跃（03:42 实测 311% CPU） → 锚 **03:42** | 软检 T+35 = 04:17（理论值） | 实际预期 03:57–04:03 自然收轮，**Tick#8 为中场观测、Tick#9 为结局 tick**；04:06 旧软检档随锚刷新作废 |

### 4.2 Tick#8 任务面（2–4 路，全部 `claude-fable-5-thinking-xhigh`）

| # | 任务 | 触发条件 | 动作 | 串并行 |
|---|------|---------|------|--------|
| T8-A | #103 门禁结局 + 合入连锁 | 门禁出结果 | 绿 → 报指挥官合 #103 + 预排功能 87 登记（秘书线，须合入后）；红 → 传话 Codex docs-only 修复；悬挂 → 查 workflow run | 与 T8-B 并行（零文件交集） |
| T8-B | X2 中场观测（零打扰） | 无条件 | `test-results` 失败目录计数 + trace mtime 活性锚刷新；若已现失败 → 按 §3.2 预备对应分支的补洞单骨架（只备不派） | 与 T8-A 并行 |
| T8-C | Codex 落库复核收尾 | T8-A 门禁绿 | §2.3 剩余两项（budget 判定 + 锚点抽查），并入 T8-A 汇报，不单开 Task | 并入 T8-A |
| T8-D | SEC 线增量传话 | #103 被指挥官合入 | 触发 T6 §5.2 口径①：传话秘书在 #116 追加 P6 增量（#103 合流 + 功能 87 登记 + 互斥纪律生效证据指针 → 本文档 §5）；#112/#116 仍人工合入 | 压后，材料齐才动 |
| T8-E | X2 结局 triage（预案） | 若 run 提前收轮 | 按 §3.2 裁决树对号入座；失败即出补洞单（定向、单 PR、base=#104 现分支叠加——属门控补洞栈场景①） | 严格压后于 T8-B 观测 |

VM 预算：Tick#8 期间唯一重载 = X2 在途；**T8-A/B/C/D 全部轻载（gh/git/ps 取证），
不新增重载** → 峰值 1 重载 + 轻载若干 ≤3 ✅。逻辑队列 = 门禁连锁、X2 结局、
#112/#116 人工窗口、T7-A（压后）、SEC-P6（压后）≈ 5 项，带内（2–6），无积压。
**Tick#8 恰逢 3-tick 界点（#6 后 +3）**——若 T8-D 触发即为看板 tick，未触发则
按 T6 §5.1 空刷不动原则顺延。

---

## 5. 同 VM 互斥纪律生效判定：**已生效且被遵守（四证）**

| # | 证据 | 实测值 |
|---|------|--------|
| 1 | **时间线零重叠** | capture run3 DONE 03:32:30（run3.log 末行）→ mp4 转档完成 03:34（out/ mtime）→ X2 e2e 起跑 03:39:29（etime 反推）——两重载间隔 ≥5min，无一秒共载 |
| 2 | **进程面独占** | 本 tick 全进程扫描：无 capture-l6r.mjs、无 ffmpeg 存量；唯一重载 = X2 playwright（双 worker + chromium SwiftShader，GPU 进程 311% CPU 独享 4 核） |
| 3 | **负载面回落** | load 1/5/15min = 4.17/3.56/5.87 vs T6 共载期 4.94/9.50/9.00——**5min 均值降 62%**，1min 值 ~4.2 即单路全量 e2e 正常水位；15min 值 5.87 仍带共载余波，趋势向下 |
| 4 | **顺序符合裁决** | T6 §3-2 裁决「capture 线先收尾 → X2 独占复跑排后」与实际发生顺序完全一致；X2 并未抢跑（capture DONE 后 7min 才开跑，留足了转档窗口） |

**剩余风险与窗口保护条款（写进 Tick#8 所有任务书）**：Codex 代理仍 RUNNING，
其交付已 push、无再采需求，但若自发起 run4 补拍将破坏 X2 独占窗口——**PID 20226
退出前，本 VM 禁止一切新增 capture/ffmpeg/e2e/lighthouse 重载**；确需补拍一律
排 X2 收轮之后。若 X2 本轮 52/52，则 T6 §3 归因备忘同步坐实（#2–#5 环境定性 +
互斥规则必要性的双向反证），由 X2 收口报告引用本文档 §0/§5 作窗口独占性证据。

---

*本文档为 CC-LOOP-ADVISOR-T7 Tick#7 交付物；登记看板不在本文更新，由秘书线单源维护。*
