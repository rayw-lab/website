# CC-LOOP-ADVISOR-T9 Tick#9 扇出裁决（fanout）

- **顾问**：CC-LOOP-ADVISOR-T9（model slug: `claude-fable-5-thinking-xhigh`）
- **时间**：2026-08-28 04:00–04:10 UTC（Tick#9 · 3n 看板界点 + X2 结局 tick）
- **基线**：main @ `88097f9`（本 tick `git fetch` 实测，无新合入）
- **纪律**：零 src/ 改动，本文档为唯一交付物；产出于独立 worktree `/tmp/t9-wt`
  （base = origin/main），未杀任何进程（PID 20226 全程存活）、未代任何一路 push
  业务代码。登记看板单源仍为 `docs/research/cyber-city-score-loop-orchestration.md`。

---

## 0. 事实核查（fresh 取证，全部本 tick 04:00–04:10 实测）

| 对象 | 实测状态 | 取证方式 |
|------|---------|---------|
| main | `88097f9`，无新合入 | `git fetch` + `git log origin/main` |
| **X2 e2e** | **在飞**：PID 20226 etime 30:35（04:07 快照），已超 17–23min 常规窗——失败用例的 driveTo 超时（300s+600s 级）烧掉的墙钟；04:02–04:06 反馈族（boost/brake/jump/toast/flip）截图为 spec 内 `page.screenshot` 常规落盘，非失败。`e2e-results.json` 未出 | `ps -o etime` + `ls -lat test-results/` |
| **CITY-EXP-01 失败坐实** | 03:57 落盘目录含 **`error-context.md`(20KB) + `trace.zip`(31MB)**——config `trace: 'retain-on-failure'`，两件套 = 失败铁证（T8 §3.1 判据原文兑现）。断言：`泊车位 (-28,-28) 应可达（实测 x=19.4 z=-32.7）`（`e2e/cyber-city-explore.spec.ts` L296 driveTo 600s 超时） | error-context.md 实读 + config 源码 |
| **终局失败（非 retry 中）** | `playwright.config.ts` L19 `retries: process.env.CI ? 2 : 0`；`/proc/20226/environ` 实测**无 CI 变量**（仅 `E2E_PORT=4499`）→ retries=0，无 `-retry1` 目录 → **该失败已终局，不存在翻盘 retry**。本轮 52/52 硬门已破，无需等收轮即可定性 | environ 实读 + 目录扫描 |
| T8「附件目录≠失败」复核 | **对目标线目录成立**：03:47「目标线…折叠偏好」目录仅 `attachments/`、零 trace/error-context = 通过（`testInfo.attach` 常规产物）。但 03:57 探索计数目录性质不同（上行两件套），二者不可混判 | 双目录逐项比对 |
| **X2 branch 世系** | #104 tip `c24c7f3` **已含 main `88097f9`**（`merge-base --is-ancestor` 实测）——R4 kickoff 已完成 rebase，本轮 e2e 被测树 = main ⊕ X2，失败归因域收窄到 X2 自身 diff（10 文件，src 面 = CityBlocks/FacadeKit/ForegroundFraming/StreetProps/index） | merge-base + `git diff 88097f9..HEAD --stat` |
| e2e 测试面零改动 | `git diff 88097f9..HEAD -- e2e/` 空 → 失败不是测试面改出来的 | diff 实测 |
| #103 | OPEN、`isDraft=false`、tip `1a4296f` 门禁 SUCCESS、MERGEABLE；落库审计 GO（#120） | `gh pr view 103` |
| #116 / #121 | #116（SEC-P5，tip `1c7080e`）门禁 SUCCESS；**#121（SEC-P6，tip `b7dc652`，base=main）已在途且 P5 tip 是 P6 的祖先**——秘书线 P3→P4→P5→P6 同一分支世系线性延展，合 #121 即落全部增量 | `gh pr view` + `merge-base --is-ancestor` |
| 看板现值（main 单源） | 综合 **80** / 视觉 **73** / 功能 **84** / 性能 **—**（板面仍标 Loop 8 @771b1e4，#109/#112/#116/#121 刷新均未合流） | 看板文件实读 |
| VM 负载 | X2 双 worker 仍为唯一重载；窗口保护条款延续，本 tick 顾问线零新增重载 | ps 实测 |

---

## 1. X2 结局 triage（任务 ①）

### 1.1 核心判定：T8 三分支树已可对号入座——「复挂」分支命中，且归因升级为「坐实」

T8 §3.1 树的分支 2「仅 CITY-EXP-01 复挂 → 真回归嫌疑」**已提前兑现**（无需等收轮）：
失败终局（§0 retries=0 证据链），且本 tick 完成了 T8 补洞单模板「排查顺序①②」的
前置工作——**trace 末帧 + 几何比对已把嫌疑锁到 X2 两处新增碰撞面**：

**证据 A（trace 末帧，`trace.zip` 内 resources 最后 screencast 帧实读）**：
车以 17 km/h 持续顶死在楼排墙角，出生泊位触发圈（autodrive-lab, 28,-28）就在旁边
——车基本没离开出生区即被弹离航线、楔死（油门在踩、位移为零 = 经典 collider 卡死）。

**证据 B（几何比对，X2 diff 内坐标 vs spec 走廊纪律）**：
CITY-EXP-01 走廊纪律（spec L279 注释原文）＝「直线走廊 z∈[-24,-28]，避开隔离墩阵
|x|,|z|≲18 / 灯杆线 ≥8m / 楼排 z≤-32」。X2 本批两处新碰撞体恰在此域：

| 嫌疑 | 坐标（世界系） | 与走廊关系 |
|------|---------------|-----------|
| **`ForegroundFraming` 桥腿柱** | 右腿 **(15.7, -26)**、左腿 **(-15.7, -26)**，cuboid 半宽 [0.62, 6.7, 0.62]，z 占位 [-26.62, -25.38] | **正压走廊带 z∈[-24,-28] 中线**。东段：出泊位 (24.5,-24.5)→(-28,-28) 直线在 x=15.7 处 z≈-25.1，与腿柱北缘间隙 ~0.3m ＜ 车半宽 → 必蹭。西段：spec L426 第二条腿 (0,-24)→(-28,-28) 直线在 x=-15.7 处 z≈-26.2，**正穿左腿柱 z 占位**。X2 自己的避让核对（文件头注释）只查了灯杆/广告板/泊车圈 r6/路缘 halfWidth——**没对 e2e 驾驶走廊** |
| **`StreetProps` 东北角簇** | 簇心 (19.5, -19.5)，PropBin 展开位 ≈(20.8, -20.8)（含随机 rotY） | 出泊位倒车→左转弧线（(28,-28)→(24.5,-24.5)→西向）掠过其南缘，间隙 ~2m 量级，转向余量内有接触风险。车最终卡位 x=19.4 与簇线 x≈19.5 同线，卡点 z=-32.7 已陷楼排禁区 |

第一触点是腿柱还是 PropBin 由 trace 回放定案（补洞段职责）；**但两者均为 X2 新增、
均在走廊域内，无论哪个先触，归因都落 X2 本批 diff**——「真回归」不再是嫌疑而是坐实。

**连环失败预警**：左腿 (-15.7,-26) 正穿 spec L426-428 第二条探索用例（预置进度→
完成闭环）的 leg2 直线。该用例在本轮后段仍要跑——**收轮 JSON 可能出现探索族第 2 个
失败，属同根，不另开单**。

### 1.2 结局三分支树（收轮 JSON 落盘后对号，取代 T8 树完成使命后的下一层）

前提：`e2e-results.json` 落盘 + `ps` 确认 PID 20226 退出。**失败计数只认 JSON
（`readFileSync` 读，Node 22 ESM import 断言坑），不认目录数**。

| 分支 | 判据 | 裁决 |
|------|------|------|
| **A（基线预期）**：失败 = 仅探索族 1–2 例，特征均为 driveTo 超时 + 卡位坐标贴 §1.1 两嫌疑 | 失败用例名 ∈ {CITY-EXP-01, 探索完成闭环}，error 均为「应可达」断言 | **同根单因** → 开 `cc-vis-x2-plug-5b71`（§1.3），单洞单补；#104 维持 draft，T7-A 顺延 |
| **B**：探索族之外另有失败（feedback/quest/perf/visual 任一） | JSON 内其他 spec 失败 | plug 单**范围不扩批**（仍只修走廊碰撞面）；其他失败单独 triage——先按 SwiftShader 挤兑/超时假阴性排查（本轮失败用例烧掉 15min 级超时，后段用例受挤兑概率升高），确属独立回归才另开单 |
| **C**：出现 `Channel closed` / worker crash 类环境失败 | JSON error 或进程异常退出 | 沿 T8 分支 3：环境专项升级（worker dump/内存水位，T8 §0 负载快照可引）；plug 与环境专项**分开派单**，不并 |

三分支**均开 plug**（CITY-EXP-01 失败已终局、归因已锁，不存在「全绿」分支）；
差异只在是否附加动作。唯一翻案条件：trace 回放证明第一触点非 X2 新增碰撞体
（几何 + 末帧 + 终局三证下概率极低）。

### 1.3 `cc-vis-x2-plug-5b71`：**GO（条件触发）**——收轮即派

**触发**：PID 20226 退出 + JSON 落盘（预计 04:15–04:35，失败超时烧钟后推）。
**收轮前禁派**（勿杀 e2e、独占窗口保护条款延续）。

T8 §3.3 模板照用，本 tick 增补以下 delta：

```
任务：CC-VIS-X2-PLUG——探索走廊碰撞面定向补洞（CITY-EXP-01 终局失败 + 左腿连环风险）
分支：cursor/cc-vis-x2-plug-5b71，base = cursor/cc-vis-x2-facade-r2-1d6f（tip c24c7f3，
    已含 main 88097f9——栈场景①，登记栈序 + base SHA 到看板）
排查顺序（T8 模板①②已由 T9 预办，直接从定案起步）：
  ① trace.zip 回放定第一触点（仓内指针：X2 worktree test-results 探索计数目录）——
    只定触点，归因方向已锁（§1.1 证据 A/B）；
  ② 修复域 = ForegroundFraming 桥腿柱 (±15.7,-26) + StreetProps 东北簇 (19.5,-19.5)：
    腿柱必须让出走廊带 z∈[-24,-28]∩x∈[-28,24.5]（含车半宽 ~1m + 转向余量 ≥1.5m）；
    东北簇对出泊位左转弧线复核同款余量。
修复约束（设计题留给补洞段，advisory 只列约束）：
  · 不动 e2e（测试面 = 审计面，零改动实测在案）；不动隔离墩（hitCount 语义在册）；
  · 桥体构图有 NDC 取证正本（facade-kit README §NDC + ritual_idle 机位）——桥沿 z
    北移出走廊带（z≥-21）或腿柱世界 x 外扩（≥30 墙面线外）均须复跑
    tools/camera/audit-x2-visibility.mjs 探针，构图与碰撞双达标才算修完；
  · 最小修复，禁顺手调参/重构。
硬门（沿 T8 模板，不降门）：修复后独占窗口全量 e2e 52/52（0 failed/0 skipped/0 flaky）；
  提交前还原 docs/spec/assets/e2e-batch1/*.png 全部 14 张；门禁 fresh 绿；
  poster 类重拍（如有）永远排批次最后。
```

**范式沉淀（建议随 plug 报告入库）**：X2 的站位避让清单缺了「e2e 驾驶走廊」这一项
——后续所有城市摆件批次的避让核对表应把 `cyber-city-explore.spec.ts` 走廊注释
（东西走廊 z∈[-24,-28]、途径点 (0,-24) 南向进路）列为固定核对项，与灯杆/泊车圈同级。

---

## 2. #103 / #116 合流建议（任务 ②，指挥官人工窗口）

### 2.1 #103：**建议即合，不等 X2 收轮**

三要素齐（undraft + tip `1a4296f` 门禁 SUCCESS + MERGEABLE）+ 落库审计 GO（#120）
——T8 §2 材料门全过。文件域纯 docs，与 X2/e2e 零交集，X2 独占窗口不构成阻塞。
合入后立即触发秘书连锁（§4 口径）。#120 审计报告本身可随后按指挥官节奏合入留痕。

### 2.2 #116：**建议不单独合——由 #121 世系收编，合 #121、关 #116**

fresh 世系实测（§0）：秘书线 P3（`3815160`）→ P5（`1c7080e`）→ P6（`b7dc652`）
为**同一分支线性延展**，#121 base=main 且含 P5 全部增量。据此：

1. T8 §2.2「通道二择」的前提已被 P6 开分支的事实更新——现行通道 = **增量 commit
   追加到 `cursor/cc-loop-sec-p6-5b71`（#121 内）**：#103 合流登记 + X2 triage 结局
   （等收轮 JSON）并入同一 P6 增量，一次刷板，避免两次合流窗口；
2. 指挥官只合 **#121**（待其门禁转绿——04:08 快照 conclusion 尚空/在跑）；
3. #121 合入后，**#109 / #112 / #116 作 superseded 关闭**（内容已全量包含于 #121
   世系，留 PR 关闭说明即可）——防看板双源；
4. 合流顺序：**#103 先、#121 后**（#121 的最后增量要登 #103 合流事实与功能 87 上板，
   顺序倒置会造成 P6 内容再次过期）。

---

## 3. Tick#10 预排（任务 ③）

| # | 任务 | 触发条件 | 动作 | 串并行 |
|---|------|---------|------|--------|
| T10-A | **plug 派单与监跑** | X2 收轮（JSON 落盘） | §1.3 任务卡即发；plug 的独占复跑是收轮后本 VM 唯一新重载 | 首位 |
| T10-B | 秘书 P6 增量收口 | #103 MERGED ∧ X2 收轮 JSON 出 | §2.2 通道：功能 87 上板 + X2 triage 裁决行 + 3n 界点补登，一次 commit；随后报指挥官合 #121 | 与 T10-A 并行（文件域零交集） |
| T10-C | M0-R4 轻载预备单 | 沿 T8 §5：窗口门（X2 收轮后仍被 plug 复跑占用）+ 登记门（#103 合流）双查 | 仅轻载三件事（rebase + 剔 12 张截图噪声 + 锁诊断分口径）；实算重载段继续排队尾 | 最低优先 |

### T7-A 视觉审计触发条件（沿 T7 §3.3 五条件 + 补洞路径修正）

T8 曾预排 T7-A「最早 Tick#10」——**该时点作废**：现实已入补洞路径，T8 §3.2 自带的
顺延条款生效（**Tick#13+**）。触发条件全清单（全过才派）：

1. plug 段落地：`cc-vis-x2-plug-5b71` 修复 commit + 补洞报告（trace 触点定案 +
   NDC 探针复跑双达标）已入 #104 栈；
2. 修复后独占窗口全量 e2e **52/52**（0 failed/0 skipped/0 flaky，JSON 为准）；
3. 14 张 `docs/spec/assets/e2e-batch1/*.png` 还原，`git diff --stat` 零截图噪声；
4. #104 undraft + tip 门禁 fresh 绿（含 LHCI 不降：`/` 与 `/home/` 四项逐项对上轮，
   本 VM null 时按 CI artifact 回填并登记来源）；
5. X2 线 IDLE（零 capture/ffmpeg/e2e 存量，ps 取证入审计前置）。

审计口径提醒：视觉双评 |Δ|≤5 检验自评；专项门以**审计独立分**判定；自评系统性
偏乐观 ~2 分（AGENTS.md 坑表）。审计走「候选 ⊕ main」集成树（栈上 PR 纪律）。

---

## 4. SEC-P6 登记矩阵口径——#103 未合窗口（任务 ④）

### 4.1 四行口径（#103 未合期间，P6 增量必须照此写）

| 维度 | 北极星 | 生产登记 | 在途注记（不入登记列） |
|------|--------|----------|----------------------|
| 综合 | **98** | **80** | M0-R4 排队尾（诊断分口径） |
| 视觉 | **98** | **73**（板面单源，#94 世系） | X2 复挂 → plug 段（§1.3）；T7-A 顺延 Tick#13+ |
| 功能 | **90** | **84** | **#103 审计 GO（#120）待指挥官合流；87 禁止预登** |
| 性能 | **85** | **—** | 解锁条件不变：真机 human-gate 六腿 → AL-PERF |

**铁则**（T8 §2 触发条款重申）：功能 87 上板的唯一触发 = `gh pr view 103` == MERGED。
ready + 审计 GO + 门禁绿 ≠ 触发；未合窗口内 P6 只能把 87 写进「在途」列注记，
生产登记列保持 84。合流当 tick 即按 T8 §2.3 预填清单一次性登 87（Δ 收敛 +6→+3）。

### 4.2 视觉行 71/73 分歧点名（口径卫生）

AGENTS.md 四行模板快照写「80/71/84/—」，而 main 树看板单源登记矩阵为视觉 **73**
（#94 AL-VIS 71→73 归档在案）。二者取**看板单源 73**（AGENTS.md 自身条款「见看板
登记矩阵表」指回单源；71 为 #94 合流前的过时快照）。建议：SEC-P6 增量或父代理
直改白名单（文档措辞类）顺手把 AGENTS.md 该行刷成 73 并注明世系，消除双源漂移；
指挥官如另有最新口径，以其明示为准。

### 4.3 X2 失败的登记时点

CITY-EXP-01 失败虽已终局（§0），但 P6 登记 X2 行必须**等收轮 JSON 全量计数**
（失败总数、是否连环左腿失败、有无 B/C 分支附加项）——目录数不作登记依据。
登记措辞建议：「X2 e2e 复挂：探索走廊碰撞面回归（桥腿/角簇，归因坐实），plug 段
`cc-vis-x2-plug-5b71` 开单（栈场景①，base c24c7f3）；#104 维持 draft」。

---

## 5. 本 tick 顾问裁决一览（供父代理直接执行）

1. **X2**：勿杀勿扰，等收轮；收轮即按 §1.2 树对号 + §1.3 派 plug（GO 已裁，条件触发）。
2. **#103**：报指挥官即合（材料齐 + GO），不等 X2。
3. **#116**：不单独合；#121 世系收编，#103 → #121 顺序合，随后 #109/#112/#116 关闭。
4. **SEC-P6**：增量 commit 追加进 #121；未合窗口四行口径照 §4.1；X2 行等 JSON。
5. **T7-A**：Tick#10 不触发，顺延 Tick#13+，五条件照 §3。
6. **VM 预算**：收轮前零新重载；收轮后 Tick#10 唯一新重载 = plug 独占复跑，峰值 ≤2 ✅。

---

## 6. 收轮后增补（04:13–04:22 UTC fresh，Tick#9 半程追加）

本节在 §0–§5 成文并推送后落笔：X2 主运行于 ~04:13 收场，结局形态与 §1.2 预设有
两处重大出入，据实增补（§1–§5 原文不改，冲突处以本节为准）。

### 6.1 主运行非自然收轮——中断定性

run2 日志（`/tmp/x2-e2e-run2.log`，X2 线自有 tee 留痕）04:12 后**无最终汇总行即断**；
`playwright-report/index.html` 与 `e2e-results.json` 同于 04:13:05 落盘，后者 stats =
`{expected:0, skipped:80, unexpected:0, duration:348ms}`——**中断产物，禁止用作任何
门禁/登记计数**（80 为全量用例数，中断路径把全部标 skipped）。顾问线全程未触碰
PID 20226（勿杀纪律遵守），中断来自 X2 线自身动作或其 shell 块超时。

**新坑两条（建议随 plug 报告入手册 §3.5 坑表）**：
1. 被中断的 playwright 会**覆写** `e2e-results.json` 为无效计数——收轮判定必须
   「进程自然退出 + list 日志末行汇总 + JSON stats」三证合一，缺一不认；
2. 35min 级全量 e2e 勿裸跑在可能被超时掐断的前台 shell 块——tmux/后台化 + 自然收轮。

### 6.2 失败清单定稿（中断前 list 日志 ✘ 行 + trace 两件套互证）

| 用例 | 时长 | 报错 | 归因层 |
|------|------|------|--------|
| CITY-EXP-01 ✘ | 14.8m | 泊车位 (-28,-28) 不可达，卡死 (19.4,-32.7) | **走廊碰撞坐实**（§1.1 原判不变） |
| CITY-QST-02 ✘ | 23.3m | `driving 空闲 30 设计秒应打 idle-nudge` 埋点未打 | **异根嫌疑**：X2 diff 未触 quest/idle 面——挤兑/设计秒计时线 |
| CITY-FB-01…09 ✘ | 15.2m | 900s 硬超时（无具体断言） | **异根嫌疑**：长链挤兑（EXP-01 烧钟期为共存窗口） |
| 键位卡 ✘ | 04:13 | `locator.click: Test ended.` | **中断窗口误伤嫌疑**——run 被掐时正在跑，暂不计真失败，复跑核实 |
| 触屏文 ✘ | 04:13 | 挂载态 `robot_idle` 断言 | 同上，中断窗口误伤嫌疑 |

连坐面：EXP-02 / FB-05 / FB-06 serial 连坐 skip；world-perf / city-perf / visual
三个殿后 project 因 dependencies 链**整体未跑**。另：全量用例数实测 **80**（config
7 projects），「52/52」为套件增长前的旧口径——硬门判定按「0 failed / 0 skipped /
0 flaky 全量通过」执行，总数以自然收轮 JSON 为准，建议秘书线顺手把硬门表述刷新。

### 6.3 树对号：**分支 B 成立** + 对照跑判读表

§1.2-B 原文执行：plug 单范围仍锁走廊碰撞面**不扩批**；QST-02 / FB 族先按挤兑与
中断复核，确属独立回归才另开单。

**X2 线已自发开 main 树对照跑**（04:13:57 起，`/tmp/main-wt`，port 4501，单跑
CITY-EXP-01，ETA ~04:25–04:30）——正是 §1.1 归因的终审实验，Tick#10 首项对号：

| 对照跑结局 | 判读 | 动作 |
|-----------|------|------|
| main 绿 | X2 新碰撞面归因**终审坐实** | §1.3 plug 即派（GO 生效） |
| main 也挂 | 归因翻案：driveTo 控制器 / 环境慢线 | plug 暂缓，改开环境/控制器专项；§1.1 几何证据转为「叠加因素」降级归档 |

### 6.4 预算与登记修正

- VM：收轮后新重载配额已被对照跑占用；plug 独占复跑排其后，且**必须自然收轮取数**。
- SEC-P6 X2 行登记措辞（替代 §4.3 建议稿）：「X2 run2 04:13 中断收场（JSON 无效，
  以中断前 list 日志计数）：确证失败 3（EXP-01 走廊碰撞坐实 / QST-02 / FB-01…09
  异根待复核）+ 中断误伤嫌疑 2 + 殿后 3 project 未跑；main 对照跑在飞；plug 段
  待对照跑判读后派单」。

---

*本文档为 CC-LOOP-ADVISOR-T9 Tick#9 交付物；登记看板不在本文更新，由秘书线单源维护。*
